import React from "react";
import HomePage from "./home";

const MainPage = () => {
  return (
    <HomePage
      initNewsActivity={[]}
      initNewsComplete={[]}
      initNewsActivityStudent={[]}
    />
  );
};

export default MainPage;
